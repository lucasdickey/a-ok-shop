/**
 * Site configuration settings
 */

export const siteConfig = {
  // Featured products on homepage (product handles)
  featuredProducts: [
    "ape-logo-embroidered", // Update these with your actual product handles
    "same-vibes-but-more", // that you want to feature on the homepage
    "a-ok-the-typewriter",
  ],

  // Site metadata
  siteName: "a-ok.shop",
  siteDescription: "Apes On Keys - Nerd Streetwear (for real)",
};
